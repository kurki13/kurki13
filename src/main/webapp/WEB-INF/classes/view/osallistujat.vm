#parse ("hello.vm")

#if($session.getAttribute('selected_course_id'))
    
    Valittu kurssi: $valittuKurssi.listaString() <br>
    Kurssin osallistujat: <br>
    <ul>
    #if($valittuKurssi)
        #set ($i=0)
        #set ($opiskelijat = $session.getAttribute('selected_course_students'))
        #foreach($op in $opiskelijat)
            <li>$op.etunimi $op.sukunimi
            #set ($i=$i+1)
        #end
    #end
    </ul>
    Results: $i

#end
</body>
<html>