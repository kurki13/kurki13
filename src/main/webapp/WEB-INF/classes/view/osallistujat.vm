<html>
<body>
#set($qs = $request.getQueryString())
#if($qs)
    #set($params = $queryparser.split($qs))
#end

#if($session.getAttribute('selected_course_id'))
    #set($kur = $KurssiKyselyt.kurssiIDlla($session.getAttribute('selected_course_id')))
    Valittu kurssi: $kur.listaString() <br>
    Kurssin osallistujat: <br>
    <ul>
    #if($kur)
        #set ($i=0)
        #foreach($os in $OsallistuminenKyselyt.listaKurssinOsallistumisista($kur))
            #set($op = $OpiskelijaKyselyt.opiskelijaHetulla($os.hetu))
            <li>$op.etunimi $op.sukunimi
            #set ($i=$i+1)
        #end
    #end
    </ul>
    Results: $i
#end
</body>
<html>