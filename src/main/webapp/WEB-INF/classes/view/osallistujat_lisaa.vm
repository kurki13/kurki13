#set($sukunimi = $params.get('filterSukunimi'))
#set($hetu = $params.get('filterOpnro'))
#set($ryhm = $params.get('ryhma'))
#set($haetut = $OpiskelijaKyselyt.haeOpiskelijat($hetu, $sukunimi))

<table class="table table-condensed table-bordered" style="max-width:1000px">
  <caption>
   Opiskelijoita valittu $haetut.size()
  </caption>
  <tr>
    <th> Nimi (syntymäpäivä) </th>
    <th> OpNro </th>
    <th> Ryhmä </th>
    <th> Ilmoita kurssille </th>
  </tr>     

   #foreach($opiskelija in $haetut)
   <tr>
    <td>
      <b><a href="mailto:$opiskelija.sahkopostiosoite">$opiskelija.etunimi, $opiskelija.sukunimi</a></b>
    </td>
    <td>
      <center><tt>$opiskelija.hetu</tt></center>
    </td>
    <td>
      <center>
      <form action="index" name="addForm49" method="post" class="inline" style="margin:0;display:inline;">
	<select name="group" class="form-control form-inline" style="width:70px">
        #foreach($ryhma in $ryhmat)
            <option #if($ryhm==$ryhma)selected#end>$ryhma</option>
        #end
        </select>
         <input type="hidden" name="TS" value="??????????"> ##MIKÄ ON TS?!
         <input type="hidden" name="ssn" value=$opiskelija.hetu>
         <input type="hidden" name="doAdd" value=$opiskelija.hetu>
      </form>
      </center>
    </td>
    <td>
      <center>
      <form action="index" method="post" class="inline" style="margin:0;display:inline;" onSubmit="return false">
         <input type="button" value="lisää" class="btn btn-success btn-sm"
                onClick="document.addForm49.submit()">
      </form>
      </center>
    </td>
  </tr>
  #end
  </table>
