#parse ("hello.vm")

$request.getParameter('hetu') <br>

#foreach($opiskelija in $OpiskelijaKyselyt.opiskelijatHetulla($request.getParameter('hetu')))
    $opiskelija.hetu
    $opiskelija.etunimi
    $opiskelija.sukunimi
    $opiskelija.osoite

#end

</body>
<html>