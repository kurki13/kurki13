#parse ("hello.vm")

$params.get('hetu') <br>
#foreach($opiskelija in $OpiskelijaKyselyt.opiskelijatHetulla($params.get('hetu')))
    $opiskelija.getValue($opiskelija.getHetu())
    $opiskelija.getValue($opiskelija.getEtunimi())
    $opiskelija.getValue($opiskelija.getSukunimi())
    $opiskelija.getValue($opiskelija.getOsoite())
#end

</body>
<html>